language: cpp
dist: bionic
osx_image: xcode10.2
before_install:
- |-
    case $TRAVIS_OS_NAME in
      windows)
        choco install -y make mingw
    esac
compiler:
- gcc
- clang
os:
- windows
branches:
- master
- development
